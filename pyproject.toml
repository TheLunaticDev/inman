[project]
name = "inman"
version = "1.1.0"
description = "The basic inventory manager app."
dependencies = [
    "flask", "pdfkit",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
